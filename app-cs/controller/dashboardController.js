angular.module('business-insights').controller('dashboardController', ['$state','$localStorage', '$scope','$http', 'ngDialog', function($state,$localStorage,$scope, $http, ngDialog){
 
// if($localStorage.accessToken){ }else{$state.go('sign');}


 }]);